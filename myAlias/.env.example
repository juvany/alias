# Base44 app ID — found in base44/.app.jsonc after running `base44 login`
# Copy this file to .env and fill in your app ID to enable custom word packs.
# Without this, the game works fully offline with built-in words.
VITE_BASE44_APP_ID=
